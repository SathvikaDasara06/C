#include<stdio.h>
int main(){

    int a,b,i,j,sum;
    
    printf("enter the array a:");
    scanf("%d",&a);
    printf("enter the array b:");
    scanf("%d",&b);
    
    sum=(a+b);
    printf("the sum of the two numbers is %d", sum);
    
    return 0;
}